SELECT number1 % number2 AS mod
FROM decimals;

SELECT MOD(number1, number2) AS mod
FROM deciamls;

--это варинаты одного и того же
--Modulus (mod) — это операция, которая возвращает остаток от деления одного числа на другое.
--Например, если вы разделите 10 на 3, то результат будет 3, а остаток — 1, который и является результатом операции modulus.